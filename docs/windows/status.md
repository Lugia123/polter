# Windows 移植 · 当前状态

**这一篇是「走到哪了」，会随进度改写。** 为什么这么做见
[design.md](design.md)，怎么动手见 [development.md](development.md)——那两篇
是设计和方法，这一篇是进度和欠账。

目标：**一个 `polter.exe`，在 Windows 上能用，能力对齐 macOS。**

> 最后更新：2026-08-31。下面每条结论都标了是实测还是推理；
> **凡是没标的，默认没验过。**

## 一、今天从哪走到哪

| | 早上 | 现在 |
| --- | --- | --- |
| 测试套件 | **在 Windows 上编不出来** | 能编、能跑，有确切失败清单 |
| DLL | 从没加载过 | 加载 + 函数调通（`ghostty_info()` 返回自己构造的版本串） |
| C API 初始化 | **被上游显式挡死**（没人知道，因为从没人调过） | 已拆，**不动 ABI** |
| 屏幕 | 一个像素没画过 | **窗口里跑着 cmd，提示符和光标都在** |
| ConPTY | 第 4 层从无测试覆盖 | **产品路径已证明**，外加一个活跃的自动化测试 |
| 监管层 IPC | Unix socket 崩溃 | **命名管道 3/3 跑通整个 server 套件** |
| 品牌 | 全是 Ghostty | 宿主已改 Polter |

## 二、关停死锁：已修，两种时序下都验过【实测】

**结论**：命名管道传输层在 Windows 上跑通整个 server 套件。

| 版本 | 时序 | 结果 |
| --- | --- | --- |
| 修复前（`pD`） | 带插桩 | **3/3 挂**在 `stop.failInflight.post` |
| 顺序修复（`pE`） | 带插桩 | **3/3 挂**在同一行（sha 已核，修复确实在里面） |
| 改异步（`pF`） | 带插桩 | **3/3 panic**（shutdown 首次通过） |
| 删掉 `CancelIoEx`（`pG`） | 带插桩 | **3/3 过** |
| **同上，交付版（`pI`）** | **不带插桩** | **5/5 过**，`92 passed; 5 skipped; 0 failed` |

**为什么两种时序都要验**：插桩会改变时序，而原始故障是竞态——「确定性复现下修好了」
不蕴含「竞态版也修好了」，而交付的正是不带插桩那个。

**为什么是 5 次不是 3 次**：原始形态是 2 挂 1 过。三次全过是碰巧的概率，
按**观察值**（通过率 1/3）算是 3.7%，按**保守假设**（通过率 2/3）算是 **约 30%**；
五次分别是 0.4% 和 13%。**「保守估计约 30%」这个限定词不能省——去掉它就成了
一个听起来像测出来的数。**

**这个结论的边界**：5/5 过**不等于**没有竞态，只等于在这个基率下没观察到。
准确的说法是：**若仍有竞态，其发生率显著低于原来的 2/3。**

## 二之二、下一步（按性价比排，都有判据）

1. **M3 中文输入**：TSF 探针已验可行（约 470 行）。接进真终端只差三件，**都不需要动 C API**：
   UTF-16↔UTF-8 换算、宽度表调 `ghostty_unicode_grapheme_width`（已导出、真机验过
   「你」=2 格）、滚动时 `imePoint()` 的 TODO。
2. **M6 能力对齐**：72 个 action 里要实现 54 个（14 必须 + 40 对齐），宿主现在实现了 14 个。
   参照是 `Ghostty.App.swift`——2,515 行里只有 44 行碰 `NS*`，基本是「action 的 switch + C 调用」。
3. **`Surface.zig:1047` 的冲突要裁**：实测「无害且无用」（主线程 82 次 `drawFrame` 零可观测
   差异，缩放期间的重绘只来自渲染线程，表现为一圈黑边）。两个选项见第五节。
4. **libghostty 的日志在真机上看不见**：`std.log` 在 lib artifact 里没落到进程 stderr。
   `ghostty_init` 那次失败只拿到返回码 1，根因是读源码找的。**不修，后面每个真机故障
   都要重复这个成本。**
5. **M5 插件**：**等拍板解释器**，不定排不进去。

## 三、四条线的最终状态

| 线 | 交付 | 状态 |
| --- | --- | --- |
| #54 | 命名管道传输层（`transport*.zig` + `Server.zig` + `os/windows.zig`） | ✅ 3/3 通过，欠一次无插桩确认 |
| #55 | `GHOSTTY_PLATFORM_WIN32`、B6 尾点与保留设备名 | ✅ 完成 |
| #56 | TSF 探针、DLL 加载验证、ConPTY 测试（A 活跃 / B skip） | ✅ 完成 |
| #57 | WGL、`ghostty_init` 闸门、M1 宿主、品牌 | ✅ 完成 |

## 四、下一个里程碑要做什么

**M3 中文输入**：TSF 探针已验可行（约 470 行）。接进真终端的三个已知欠账：
UTF-16↔UTF-8 换算、**宽度表必须调 `ghostty_unicode_grapheme_width`**（已导出、真机验过，
「你」=2 格），以及滚动时 `imePoint()` 的 TODO。**不需要动 C API。**

**M6 能力对齐**：72 个 action 里必须实现 54 个（14 必须 + 40 对齐），目前宿主实现了 14 个。

**M5 插件**：**等用户拍板解释器**。出厂 9 个插件里 8 个是 `.sh`。三个选项和代价见
`development.md` 5.3。**这一条不定，M5 排不进去。**

## 五、悬着的技术账（都有确切位置）

1. **`Surface.zig:1047` 的冲突**：实测「无害且无用」——主线程 82 次 `drawFrame` 零可观测差异，
   缩放期间的重绘只来自渲染线程（表现为一圈黑边）。两个选项：让上下文按帧易主
   （每帧一次 `makeCurrent`，真正满足契约），或明确写下「WGL 不支持主线程 drawFrame」。**未裁。**
2. **libghostty 的日志在真机上看不见** —— `std.log` 在 lib artifact 里没落到进程 stderr。
   `ghostty_init` 那次失败只拿到返回码 1，根因是读源码找的。**不修，后面每个真机故障都要靠猜。**
3. **ConPTY 测试 harness**：`ConPTY B` 仍 skip。已定性为「控制台归属未检验，仍是最有希望的候选」
   （`GetConsoleCP=936`，测试进程确实附着控制台；m1host 是 GUI 子系统所以能工作）。
4. **品牌六处未做**：AppUserModelID（不设的话换目录就变两个任务栏图标，且决定 toast 身份）、
   VERSIONINFO 资源、单实例互斥体名、注册表键、崩溃上报名。
5. **`Command: custom env vars`** 在 Windows 上失败，**已降级为非阻塞**（生产路径是整份环境照抄）。
   根因未定位，探针 `C:\app\envprobe.exe` 已就位没跑。

## 六、今天证伪或修正的判断（九条，五条是主控的）

| 谁 | 原来 | 实际 |
| --- | --- | --- |
| 文档 | `poltergeist` 34,046 行是跨平台的 | 编得过，**跑不起来** |
| 文档 | 渲染是唯一真正的未知数 | 渲染已通，未知数是**没有 apprt** |
| 文档 | Rust 壳一两万行 | 基数错 32%，**92% 参照代码碰平台框架** |
| 文档 | 73 个 action | **72 个**（`action.zig` 里有两个同名 enum） |
| 主控 | `ime_point` 是宿主给核心一个点 | **反的**，核心给宿主矩形，且更乐观 |
| 主控 | `Transcript` 是安全漏洞 | **不是**（白名单一直防着）；真危害是尾点和保留设备名 |
| 主控 | `custom env vars` 掐断认证链 | **链断了**，生产是整份环境照抄 |
| 主控 | 「回归全绿」是充分标准 | 测试**够不着** `embedded.zig` 和 `OpenGL.zig` |
| 主控 | 控制台归属修好了输出路由 | **两个变量同时变了**，起作用的是另一个 |

**九条没有一条是靠讨论纠正的，全是靠一次具体的测量。**

## 七、方法论（每条都带触发条件，今天真踩出来的）

0. **拿到一个支持结论的读数就停手——这是下面几条的母形状。**
   今天被推翻的九条判断里，多数不是因为没有证据，而是**证据就在手里，
   而人停在了让结论成立的那一步**：
   - 令牌下发那条：链的**下游**读了（env 怎么被消费），**上游**没读（env 怎么被构造）
   - 目录逃逸那条：失败消息读了（`expected 2, found 0`），**那个断言在数什么**没读
   - 控制台归属那条：看到测试转绿就归因给它，**没注意同一轮还有第二个变量在变**
   - action 计数那条：拿到一个数，**因为它和文档一致就不再追**（而文件里有两个同名 enum）

   **触发条件很具体：当一个读数刚好支持你正想成立的那个结论时，那就是要多走一步的时刻。**
   相关的一条：**升级一个结论（从「疑似」到「阻塞项」）比提出它需要更多证据**，
   而升级往往发生在最想收工的时候。

1. **编译挡着链接，链接挡着运行，运行挡着绘制。** 今天穿了五层，最后一层是上游显式挡的。
2. **「那个假设没被检验过，不是被排除。」** 未经检验的假设被当成已排除，比已知未解更危险——没人会再回去看它。
3. **用错的探针得到的阴性，和没做过实验是一回事。** `GetConsoleWindow` 对「无控制台」和
   「有控制台无窗口」都返回 null。
4. **负对照必须在过滤之前取。** `Where-Object` 之后再数，数的是过滤后的世界。
   同理适用于「查产物里有没有某个字符串」：**「0 处命中」和「这个搜索根本找不到东西」
   长得一模一样**。核对交付版不含调试插桩时，要同时搜一个必然存在的符号作地板——
   `插桩串: 15 → 0，而 DisconnectNamedPipe: 1 → 1`，后半句才让前半句成立。
5. **当一个阴性结果让你少干活时，那就是必须量基率的时刻。**
6. **当一条二手前提能决定下一步做什么时，先花两分钟核它。** 核它一次 `grep`，不核一个完整往返。
7. **预先声明判据，是为了在结果出来之前锁住解释权。**
8. **一直在调整一个不该存在的东西的参数。** `CancelIoEx` 三轮错了三种方式，正确动作是删掉它。
   每一步都对，方向整体是错的。
9. **规则的适用条件比规则本身更难记住。**「别在锁里做系统调用」是对的，但那把锁正是防
   use-after-close 的机制。
10. **绕开一条路，不等于绕开它底下的那一族问题。**

## 八、可复核的现场

- 测试机 `C:\app`：`tsf-probe*`、`dllprobe\`、`conpty\`（8 个二进制 + 输出）、
  `wintest\{pipe,pipe1,pipe80,pA,pB,pD,pE,pF,pG}`、`glprobe*`、`envprobe.exe`、`polter-host*`
- 干净基线：`d12e8452b` 上 `3975/3991`（16 skipped，0 failed）
