# Words a person is likely to type, and the command the core actually publishes.
#
# **This is an index, not a naming.** Nothing here renames anything: each line
# says "if they type the left, they mean the right". A line whose right-hand
# side no longer exists therefore degrades to today's behaviour -- the search
# simply does not match -- rather than pointing at the wrong command. The
# startup log reports how many lines have no matching command, so a stale one
# is visible rather than silent.
#
# Every line came from comparing macOS's MainMenu.xib against the core's own
# command list; 20 of that menu's 69 commands are the same action under a
# different word. Adding a line from memory is exactly what the Zig test
# `test "palette synonyms name real commands"` exists to catch.
#
# Format: <what they type> = <the core's title>
find = Start Search
search = Start Search
find next = Next Search Result
find previous = Previous Search Result
hide find bar = End Search
use selection for find = Search Selection
copy = Copy to Clipboard
paste = Paste from Clipboard
paste selection = Paste from Selection
zoom split = Toggle Split Zoom
select split above = Focus Split: Up
select split below = Focus Split: Down
select split left = Focus Split: Left
select split right = Focus Split: Right
select previous split = Focus Split: Previous
select next split = Focus Split: Next
return to default size = Reset Window Size
preferences = Open Config Using OS editor
settings = Open Config Using OS editor
secure keyboard entry = Toggle Secure Input
jump to selection = Scroll to Selection
terminal read-only = Toggle Read-Only Mode
toggle full screen = Toggle Fullscreen
full screen = Toggle Fullscreen

# ---------------------------------------------------------------- menu labels
#
# **The other half of the same problem.** The lines above are English words a
# person might type instead of the core's own. These are the words this host
# puts on its own menus -- in Chinese -- and without them somebody who reads
# «关闭窗口» off the menu and types those four characters into the palette gets
# nothing, while `close` lists five commands they cannot name. The menu is the
# most likely place a person learned the word, so it is the least likely word
# to be missing here.
#
# **Generated from the menu table, not typed from memory.** Every line below is
# one row of `menu.rs`'s table paired with the title the core publishes for
# that row's action, and `test "menu labels reach the palette"` in
# `src/input/command.zig` fails if a row loses its line -- or if a line names a
# title the core does not publish.
#
# Six menu rows have no line and cannot: `toggle_command_palette`,
# `toggle_quick_terminal` and the four `resize_split:<dir>,10` rows name
# actions **the core publishes no palette command for**, so there is nothing
# for a search to find. That is a fact about the core's list, not an
# exemption written here, and the test computes it rather than being told.
新建窗口 = New Window
# Task 310 took the catalogue's wording for four labels, and the right-click
# menu and the strip menu were synced with it. The word each of them said
# before stays on the line under it: this file is an index, not a naming, so
# an old word costs one line and buys everyone who learned it.
新建标签页 = New Tab
新建标签 = New Tab
向右分屏 = Split Right
向左分屏 = Split Left
向下分屏 = Split Down
向上分屏 = Split Up
关闭分屏 = Close Terminal
关闭标签页 = Close Tab
关闭标签 = Close Tab
关闭窗口 = Close Window
查找… = Start Search
下一个 = Next Search Result
上一个 = Previous Search Result
隐藏查找条 = End Search
复制 = Copy to Clipboard
粘贴 = Paste from Clipboard
全选 = Select All
重置字号 = Reset Font Size
放大 = Increase Font Size
缩小 = Decrease Font Size
改标签标题… = Change Tab Title…
改终端标题… = Change Terminal Title…
只读 = Toggle Read-Only Mode
终端检查器 = Toggle Inspector
终端群聊 = Terminal Conversations
终端对话 = Terminal Conversations
将此终端设为总管 = Make This Terminal a Supervisor
设为总管 = Make This Terminal a Supervisor
监督此终端 = Toggle Supervision of This Terminal
不让 agent 碰此终端 = Keep Agents Out of This Terminal
禁止 agent 进入 = Keep Agents Out of This Terminal
# Task 322 reverted the menu to Chinese literals, so the label is the word
# again rather than a msgid the catalogue turns into it. Three rows had been
# findable only because their msgid *was* the palette's title -- identical in
# every language by construction. With the literal back, they need a line.
保持这个终端在岗 = Hold This Terminal to Its Work
保持在岗 = Hold This Terminal to Its Work
保持 = Hold This Terminal to Its Work
不许下班 = Hold This Terminal to Its Work
上 = Focus Split: Up
下 = Focus Split: Down
左 = Focus Split: Left
右 = Focus Split: Right
均分分屏大小 = Equalize Splits
等分 = Equalize Splits
最大化 = Toggle Maximize
全屏 = Toggle Fullscreen
分屏缩放 = Toggle Split Zoom
上一个分屏 = Focus Split: Previous
下一个分屏 = Focus Split: Next
重置窗口大小 = Reset Window Size
恢复默认大小 = Reset Window Size
置顶 = Toggle Float on Top
# The menu says «重新加载配置» since task 310 -- it took the catalogue's
# wording, which the palette was already using. «重载配置» is what it said
# before, and it stays: this file is an index, so an old word costs a line
# and buys everyone who learned it.
重新加载配置 = Reload Config
重载配置 = Reload Config
设置… = Open Config Using OS editor
检查更新… = Check for Updates

# The tab's own right-click menu (`strip.rs`) and the terminal's (`ctxmenu.rs`)
# are two more tables of Chinese labels, and the four words below are the ones
# they say that the main menu does not. The rest of their rows repeat labels
# already listed above -- so most of that menu was searchable by accident, and
# these four were not.
重置终端 = Reset Terminal
关闭其他标签 = Close Other Tabs
关闭右侧的标签 = Close Tabs to the Right
移到新窗口 = Move Tab to New Window
