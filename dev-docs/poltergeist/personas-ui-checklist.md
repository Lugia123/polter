# 角色界面：给人看的核对清单（macOS）

> 任务 570 的欠账凭证。**测试钉不住的三样东西在这里：布局、图标、一行太长被截断。**
> 对应代码 `macos/Sources/Features/Personas/`，自动化部分在
> `macos/Tests/Personas/PersonaMenuTests.swift`（15 条，结构 / 勾选 / 启用态 / 措辞选择）。
> 写给谁：坐在这台机器前、能看见屏幕的人。

## 为什么要有这张纸

「看看对不对」会换回一句「还行」，而那等于没验。下面每一条都是
**点哪里 → 该看到什么 → 什么样算不对**，逐条可判，答「对 / 不对 / 看不到」即可。
不对的那条把屏幕拍下来。

## 先说清楚今天能看见什么

核心侧现在**已经真的会读 `personas.json` 了**，所以：

- **没写过那个文件的机器**（默认情况）：角色清单是空的，而且界面会说
  「还没有定义任何角色」还是「还没有人报上来有哪些角色」——**这两句不一样**，
  第一部分就是验它们没串。
- **`host_class` 现在恒为 `unknown`**（认 agent 是哪一家那条还没接），
  所以第 2 条里那句提示应该是「可能要 agent 重启之后才生效」。
  **这是真状态，不是占位符**——不要因为「看起来像没做完」就记成缺陷。
- **每个槽位现在恒为 `withheld`**（还没有槽位进程去起上游）。同样是真状态。

**第一部分检查的是「基本没有数据时长什么样」**——那恰恰是真机上第一眼
看见的画面，也是最容易被做成撒谎的地方。要看有数据的样子，写一份
`$XDG_CONFIG_HOME/polter/personas.json`（没设 `XDG_CONFIG_HOME` 就是
`~/.config/polter/personas.json`）再做第二部分。

---

# 第一部分：今天就能做

**0a / 0b 是起实例，约一分钟；1–10 是看屏幕，约两分钟。**

## 0a. ⚠️ 必须用 `open -n` 起一个**新实例**，不能直接 `open`

**为什么这条排在最前面**：你此刻正开着一个 Polter 在用，而**那个实例里没有角色菜单**
（它是旧的构建）。直接 `open` 多半只是把它切到前台，于是第 1 条会报回一个
**不存在的缺陷**——菜单确实没有，但不是因为代码错了。

**做**（拷这三行）：

```sh
# 在这个仓库的根目录里跑（下面那个相对路径要这个前提）
APP=$(ls -dt ~/Library/Developer/Xcode/DerivedData/Ghostty-*/Build/Products/Debug/Polter.app macos/build/Debug/Polter.app 2>/dev/null | head -1)
echo "$APP"; ls -ld "$APP"; open -n "$APP"
```

**该看到**：`echo` 出来的路径**不是** `/Applications/Polter.app`；`ls -ld` 的时间戳是
**今天刚构建的**；然后新开一个窗口。
**算不对**：路径是 `/Applications/...`（那是你在用的那个，别在它上面验）；
时间戳是几天前（构建没出来，先别往下做）；`open -n` 报错。

> 这台机器上同时存在至少三个 `Polter.app`：`/Applications` 里你在用的那个、
> `macos/build/Debug/` 里的、以及 Xcode DerivedData 里的（还不止一份）。
> **它们长得一模一样。** 上面那行 `ls -dt … | head -1` 取的是最新的那个。

## 0b. 确认眼前这个窗口来自新实例

**别用「有没有角色菜单」来判断**——那正是要验的东西，拿它当前提就是循环。

**做**（在**另一个**终端里，或在旧实例里跑）：

```sh
lsappinfo info -only bundlepath "$(lsappinfo front)"
```

**该看到**：打印出来的路径和第 0a 步 `echo` 的那个**逐字相同**。
**算不对**：打印的是 `/Applications/Polter.app`（你在看的是旧实例，回到 0a），
或者是别的应用（先点一下新窗口让它到前台再跑一次）。

**再确认一次两个实例都在**：

```sh
ps -Ao pid,comm | grep 'Polter.app/Contents/MacOS/polter$' | grep -v ' +mcp'
```

**该看到**：至少两行不同的路径 —— 一行 `/Applications/...`（你原来那个），
一行是新构建的那个。
**算不对**：只有 `/Applications/...` 一行 —— `open -n` 没起来新实例。

**做完这份清单之后**：新实例自己关掉，**别关你原来那个**。

## 1. tab 右键菜单里有没有这一项

**做**：在任意一个标签页的**标签**上点右键。
**该看到**：菜单底部一组里有一行 **「角色」**，右边有子菜单箭头 `>`。
**算不对**：没有这一行 / 有字但没有箭头。

> 🤖 **图标那一半已经不用看了**（`PersonaIconTests`）。两条版本分支各判一次：
> macOS 26 以上每个该有图标的行 `image != nil`，26 以下**一个都没有**。
> 还要人看的是「这一行真的挂在右键菜单上」——那是接线，不是建菜单。

> ⚠️ **菜单图标只在 macOS 26 以上才有**（`NSMenuItem.setImageIfDesired` 里
> `if #available(macOS 26, *)`），所以更老的系统上**菜单里一个图标都没有是正确的**，
> 不要记成缺陷。这台机器是 26.5.1，所以这里应该有图标。

## 2. 子菜单的内容和顺序

**做**：鼠标移到「角色」上，等子菜单弹出。
**该看到**，**从上到下正好是这六样**：

1. 灰色不可点：**「可能要 agent 重启之后才生效」**
2. 分隔线
3. 灰色不可点：**「还没有人报上来有哪些角色」**
4. 分隔线
5. **「不设角色」**，**前面有对勾**
6. 分隔线 + **「角色编辑器…」**，左边一个推杆图标

**算不对**（任何一条）：
- 有**英文**出现 —— 说明中文没命中，这是个真缺陷，不是语言设置问题。
- 第 3 行写的是**「还没有定义任何角色」** —— 这两句意思不同，串了就是缺陷。
- 「不设角色」**没有对勾**。
- 灰的那两行**鼠标移上去会变蓝 / 能点** —— 它们必须是死的。
- 第 1 行写的是**「下次启动 agent 时生效」** —— 那是另一句，今天不该出现。

> 第 1 行和第 3 行是这个功能最要紧的两句：它们的职责就是**不许让「还没生效」
> 看起来像「已经生效」**。它们出错的样子是「看起来很正常」。

> 🔴 **2026-09-18 真机上第 3 行不是这一句，这里没有替它下结论。** 586 在真窗口上
> 读到的是**「还没有定义任何角色」**（菜单和编辑器都是），而上面写的「该看到」是
> 「还没有人报上来有哪些角色」。同一轮里编辑器第 3 区也是**「这个终端还没有交出去
> 任何东西」**而不是「还没有人报上来这个终端交出去了什么」；第 4 区那句**一致**。
>
> 已经取到的证据：`~/.config/polter/personas.json` **不存在**（`XDG_CONFIG_HOME` 未设）；
> `PersonaCatalog.setPersonas` 的注释说「调用它本身就使清单成为 known，包括空的」；
> `ghostty_app_personas` 的注释说「零是一个答案，不是缺席」。**所以今天这两句和代码
> 的意图是一致的，上面那两行「该看到」是它被写下来时的状态。**
>
> ⚠️ **但这张纸真正在乎的性质仍然成立**：那三句**仍然是三句不同的话**，没有串成一句。
> ⚠️ **没能排除的一件事**：「文件不存在」和「读了、里面是空的」在这条路上**没有任何
> 地方区分**——核心的注释说那个区分在 face 的 `stale` 字段上，而那是另一个面板。
> 这是不是同一个坍缩下沉了一层，586 判断不了，留着。

## 3. 长句有没有把菜单撑坏

**做**：就看上一步那个子菜单。
**该看到**：两条长中文各占**一行**，完整，没有 `…`；菜单整体宽度还算正常。
**算不对**：句子被截断成 `还没有人报上来…`；或者菜单宽得跨过半个屏幕。

> 🤖 **这一条整条交给机器了**（`PersonaMenuWidthTests`）。不是看有没有 `…`，
> 是拿 `NSAttributedString` 在菜单字体下量出宽度，加上从真 `NSMenu.size`
> 标定出来的边框开销，跟 640pt（1280pt 的一半）比。**每一种语言都量**，
> 因为长的那句往往来自译文。今天最宽的是 Base 的
> 「Agents are kept out of this terminal, so its role cannot be changed」，450.9pt。
> ⚠️ 不覆盖**角色自己的名字**——那是用户文件里的字，长度不归我们管（第 11 条还是人看）。

## 4. 终端里右键，同一套东西

**做**：在终端**内容区域**（不是标签）点右键。
**该看到**：菜单**最下面**同样有「角色」，展开后和第 2 步**逐行相同**。
**算不对**：没有 / 有但内容和第 2 步不一样（两处菜单是同一份代码建的，不一样就是接错了）。

## 5. 编辑器窗口

**做**：点「角色编辑器…」。
**该看到**：一个独立窗口，标题栏写 **「角色编辑器」**；右下角一个**「完成」**按钮；窗口可以拖大拖小。
再点一次「角色编辑器…」**不该**再开出第二个窗口，而是把已经开着的那个提到最前。
**算不对**：没弹出来 / 标题是英文或是文件名 / 没有「完成」按钮 / 尺寸拖不动。

> 🤖 **这一条 2026-09-18 在真窗口上驱动过一次**（任务 586，`tools/mac-drive.sh` +
> 一条自写的嵌套点击；macOS 26.5.1，构建 `zig-out/Polter.app` @ `07b208ecb`）。
> 观察到的值，不是「看起来对」：
>
> | 该看到 | 观察值 |
> | --- | --- |
> | 弹出一个独立窗口 | `count of windows` **1 → 2**，新窗口 `name` = `角色编辑器` |
> | 标题栏写「角色编辑器」 | 同上，AX `name of window` 逐字相同 |
> | 有「完成」按钮 | 在，截图里可见（AX 里它没有可读的 title，`description` 只是「按钮」） |
> | 窗口可以拖大拖小 | `set size to {700,800}` 成功；窗口按钮集合是 关闭/全屏幕/最小化 |
> | 再点一次不开第二个窗口 | `count of windows` **2 → 2** ✅ |
> | ……而是把已经开着的提到最前 | ❌ **没有**。先把终端窗口抬到最前（顺序 `终端, 角色编辑器`），再点一次，顺序**一字未变** |
>
> 🔴 **最后一行是一个缺陷候选，成因未定。** 排除过仪器盲：`AXRaise` 在这条通道里
> 是看得见的——我自己抬编辑器时顺序确实翻转过。所以「没看见提前」不是读不到。

> 🟡 **2026-09-18 05:3x 复查（任务 587）：它今天不复现，但这条仍然未决——因为没有地板。**
>
> 在**含 588 修复**的产物上，把终端窗口抬到最前再点第二次「角色编辑器…」，**连跑三次**：
>
> ```
> 点击时菜单首行 = 可能要 agent 重启之后才生效   ⇒ 这次点击有 target
> 点前 windowOrder = ~/…/Polter_Test, 角色编辑器
> 点后 windowOrder = 角色编辑器, ~/…/Polter_Test   ⇒ 已有窗口被提到最前
> count = 2 → 2                                  ⇒ 没有开出第二个
> ```
>
> **为什么不据此结案**：三次绿**没有地板**。要证明这条顺序判据不是空转，得把
> `PersonaEditor.present` 里的 `makeKeyAndOrderFront` 拆掉、看它会不会红——
> **那一步没做成**，卡在真机上（见下）。**没有地板的绿不算证据**，所以这里不写
> 「它其实是 589 的表现」，尽管那是目前最像的解释：586 那次缺一个判别量——当时读不到
> 「这次点击有没有 target」，而 588 新增的那条说明行正好就是这个探针。
>
> ⚠️ **另外两个已知的、当时没记下来的环境差**：586 那次 `set frontmost` 之后只
> `delay 1`（今天是 `delay 2`），而且那一次**根本没读 `frontmost`**；588 期间我
> 亲眼见过 `set frontmost of p to true` 返回正常而 `frontmost` 仍是 `false`。
> 所以「今天不复现」和「当时读错了」这两件事**今天仍然分不开**，要分开就得跑
> `delay 1` 那格对照——同样卡在真机上。
>
> 🔴 **卡在哪：没有活着的显示器时，这个 app 起得来但一个窗口都不开。** 判据不是
> 「看起来没反应」：产品自己的日志写着 `No windows open yet` 和
> `CVDisplayLinkCreateWithCGDisplays error -6661 due to invalid display count (0)`，
> `screencapture` 拍出来**整幅全黑**（它 exit=0 且写出了 156 KB 文件，**退出码在这里
> 没有判别力**），`count of windows = 0` 且点 `File ▸ 新建窗口` 之后仍然是 0。
> 归属也做过实验：在隔离 worktree 上 checkout **HEAD 干净重建**，行为一模一样
> ⇒ 不是谁的未完成改动，也不是签名。**要接着验这一条，先得有人把屏幕唤醒。**
>
> ⚠️ 顺带一条只在重建后出现、和上面无关的坑：**同一路径重建之后第一次 `open` 会被
> `Taskgated Invalid Signature` SIGKILL**（崩溃报告在 `~/Library/Logs/DiagnosticReports/polter-*.ips`），
> 而隔离 worktree 里新建的那份第一次 `open` 就起得来 ⇒ 是路径相关的陈旧 ad-hoc cdhash，
> 不是产物坏。重签要带上 entitlements，否则 8 条全被剥掉：
> `codesign --force --deep --sign - --options runtime --entitlements macos/GhosttyDebug.entitlements zig-out/Polter.app`
> （`optimize` 默认 Debug ⇒ Xcode 走 `Debug` 配置，所以是 `GhosttyDebug.entitlements`。）
>
> ⚠️ **还有一个反例值得记住**：实例**不在前台**时，同一条点击返回 `clicked` + exit 0
> 而窗口数**不变**，三次都一样；菜单项那时 `enabled = true`。唯一变量换成
> 「同一次 osascript 调用内 `AXRaise` + `set frontmost`」之后才开出窗口。成因是
> 菜单栏那条路在没有 key window 时 `PersonaMenuBar.focusedSurface` 为 nil、于是
> target 为 nil，而子菜单 `autoenablesItems = false` 让它**仍然可点**。
> 手点的人够不到（拉菜单栏时 app 必然在前台）；**驱动通道必踩**。
> ⇒ **判据永远是被测对象自己的状态变了，不是脚本打印了「clicked」。**

## 6. 编辑器里的分区，从上往下

**该看到**：
1. 顶部：这个终端的标题，下面一行小字 **「不设角色」**，再下面一行带时钟图标的小字 **「可能要 agent 重启之后才生效」**
2. **「选一个角色」** —— 下面是「还没有人报上来有哪些角色」，再下面 **「不设角色」带一个实心对勾圆圈**
3. **「这个终端交出去的东西」** —— 下面一句 **「还没有人报上来这个终端交出去了什么」**
4. **「这台机器上已经装了的」** —— **紧跟一句小字**：
   **「列在这里，是为了让你看见角色没有覆盖到哪里。Polter 不会动这些。」**
   再下面「还没有人报上来这台机器装了什么」

**算不对**：
- 第 4 区**少了那句小字** —— 那句必须在，没有它用户会去找开关，而这一区是故意只读的。
- 任何一区标题是英文。
- 第 4 区显示成一个**空列表**（什么都不写） —— 空列表会被读成「这台机器什么都没装」。
- 第 2、3、4 区那三句**「还没有人报上来…」用了同一句话** —— 它们说的是三件不同的事
  （有哪些角色 / 这个终端交出去了什么 / 这台机器装了什么），串成一句就是缺陷。

> 🤖 **分区顺序和那句必须在的小字，现在由快照钉住**
> （`PersonaEditorSnapshotTests`，参考图在 `macos/Tests/Personas/__Snapshots__/`）。
> 快照只回答「有没有变」，不回答「好不好看」——所以上面的**判断题仍然是人的**，
> 而「上次是什么样」不用再靠记忆。

## 7. 图标不是空白方块

**做**：把**编辑器窗口里**出现的图标扫一眼 —— 时钟、圆圈 / 实心对勾圆圈。
再看**菜单里**的人像、推杆、省略号、托盘（**仅当系统是 macOS 26 以上**，见第 1 条的提醒）。
**算不对**：其中任何一个是**空白**、方框、或者位置空着。

> 图标名拼错在代码里不报错，只在屏幕上少一个东西。这台机器上这 11 个名字我
> 用 `NSImage(systemSymbolName:)` 逐个查过都能取到，**但那是在 26.5.1 上查的**
> —— 一个 macOS 14 才加的符号在这里查得到、在 13 上是空的，所以这一条在**旧系统**
> 上仍然要人看。

> 🤖 **「逐个查过」这句话现在是每次跑测试都重查一遍**：那 11 个名字收在
> `PersonaSymbol` 这一个枚举里，菜单和编辑器都从它取，测试走 `allCases`。
> 手抄一份名单在测试里是会过期的，而过期的名单照样全绿。
> ⚠️ **旧系统那半依然只能人看**，理由一字未改：这台机器答的是 26.5.1 的账。

## 8. 把编辑器窗口拖到最窄

**做**：拖窗口左右边缘，缩到拖不动为止。
**该看到**：文字自动换行，上下不重叠，右边不被裁掉。
**算不对**：句子被右边缘切断 / 两行字叠在一起 / 出现横向滚动条。

> 🤖 **真窗口在最窄处量过了**（586）：编辑器**开出来就是** AX 尺寸 `460 × 452`
> ——即内容 `460 × 420` 加 32pt 标题栏，**不是** `idealWidth 520 / idealHeight 600`。
> 要它缩到 `400 × 300` 会被夹回 `460 × 452`，也就是最小尺寸正好等于 SwiftUI 里
> 声明的那两个值。截图在 `Polter_Test/586/editor-real-narrow-dark.png`（不在仓库里）：
> 文字换行、右边没被裁、没有重叠、这个尺寸下也不需要滚动。
>
> 🤖 **有一张 460×420（`minWidth`/`minHeight` 本身）的快照钉着它**
> （`theSameEditorAtItsNarrowest`）。它能保证「今天这张图不再变」，
> **不能替你判断这张图本身对不对**——录的时候人看过一次：右侧那两句
> 「手动关掉的 / 手动加上的」在最窄处仍然完整，没有横向滚动条。

## 9. 深色浅色各看一眼

**做**：系统设置切一次外观（或本来是哪个就切到另一个）。
**该看到**：灰色小字在两种模式下都读得清。
**算不对**：某一种模式下小字几乎看不见。

## 10. 护盾把开关关掉

**做**：同一个 tab 右键 →「不让 agent 碰此终端」；再右键 → 展开「角色」。
**该看到**：子菜单**顶部多一条**灰色的 **「不让 agent 碰此终端，所以它的角色也不能改」**；**「不设角色」变成灰的、点不动**；而**「角色编辑器…」仍然可以点**（看不是改）。
**做完**：再点一次「不让 agent 碰此终端」关掉护盾。
**算不对**：没有那句话 /「不设角色」还能点 /「角色编辑器…」也被禁用了。

---

# 第二部分：写了 `personas.json` 之后

⚠️ **这一段的标题以前是「核心接线之后才能做」，那句现在过期了。**
`grep -rn PENDING-W1-568 macos/` 已经是 0，而且核心那边
`poltergeist_persona_set` / `_clear` **真的会改状态了**（`2f74fb4a1`）。
所以下面有一半今天就能做，另一半还不行——**每行最后一列说的就是这件事**，
别整段跳过，也别整段当成能做。

**还不能做的那几行，是核心故意没接的，不是坏了**：单独开关一个 skill /
一个 MCP（`_skill` / `_mcp` 仍然返回 false，因为「id 过期时可见地拒绝」
那条通道没建，做一半比不做更糟）；`host_class` 认哪一家 agent；以及
槽位进程（所以 `slot` 恒为 `withheld`，`broken` / `transparent` 见不到）。

⚠️ **这一段验的是界面说了什么，不是 agent 手上拿到了什么。** 两件事走的是
两条不同的路，而它们在同一个函数里并排放着（`src/App.zig:2130`
`setSurfacePersona`）：`refreshPoltergeistTabs()` 把标记推给界面——**下面这些
行全靠它**；`wakePersonaWaits(id)` 才是去改 agent 工具面的那一条。

所以**第 12 行绿，只说明界面记住了你的选择，不说明那个 agent 重新换装了**。
换装那半是演示要验的东西，判据在 agent 那边（让它**真去调用**一个被拿掉的
工具，不要问它「你还有没有」——每轮都注入新清单，但旧清单还在对话历史里，
于是「没生效」和「生效了但它还记得」同形；roles.md 附录 A.3 栽过一次）。

顺带一条**已知且诚实**的现状，免得演示时被当成缺陷：`persona_wait` 在
「没人挂住我」那条分支上会**立刻回 `timeout:true`** 而不是挂住。它不影响
下面任何一行（这些行一条都不经过它），也不影响换装本身（唤醒换装的是变更
而不是超时）。

🔴 **12 / 16 / 20 今天一定红，而且不是界面的问题——先别拿它们去验。**

🔬 核心里 `PoltergeistMark.Persona` 只有一个构造点（`src/Surface.zig:3726`），
它**只填了 `agent_present`**，`key` 留在默认值 `null`。所以标记里永远没有 key，
于是父项永远是「角色」、永远不会变成「角色：射手」，子菜单里打勾的永远是
「不设角色」。@W4 在真机上撞到的：工具面真的变了（44→25，有负对照），
**而菜单没跟上**。@W1 正在生产端用编译期堵掉这件事。

⚠️ **第 16 行尤其要小心，它会以错误的方式红，也可能被错当成绿**：它要看的是
「勾还在、父项退回」。今天**勾从来就没有过**，所以「父项是『角色』」这一半
看起来是对的——**对得毫无意义**。只看父项的人会把它记成绿。
判据落在「那一行还打着勾吗」上，不要落在父项上。

| # | 做 | 该看到 | 算不对 | 今天能做吗 |
| --- | --- | --- | --- | --- |
| 11 | 写一份 `personas.json`（含一个中文名很长的角色），右键展开「角色」 | 角色**按文件里的顺序**逐行列出；名字长的那行不被截断 | 顺序和文件不一致；长名字被切 | ✅ 能 |
| 12 | 选中一个角色 | 该行打勾；**父项变成「角色：<名字>」** | 父项还是「角色」 | ❌ 核心还不给 `key`，见下 |
| 13 | 打开编辑器，单独关掉一个该角色给的 skill | 那一行右边出现**「手动关掉的」**；父项变成 **「<名字>（已改）」**；编辑器里出现**「恢复成这个角色的样子」**按钮 | 父项没变；写成「手动加上的」（方向反了） | ❌ `_skill` 未接 |
| 14 | 单独打开一个该角色**没给**的项 | 右边写 **「手动加上的」** | 写成「手动关掉的」 | ❌ 同上 |
| 15 | 点「恢复成这个角色的样子」 | 两个标记都消失，父项回到「角色：<名字>」，按钮自己消失 | 按钮还在（说明偏离没清干净） | ❌ 同上 |
| 16 | 在这个终端里**关掉 agent**（退出 claude），再看菜单 | 该角色那行**仍然打勾**，但**父项退回「角色」**，子菜单多一条「这个终端里没有 agent 连着，角色存下了，但还没有人穿上」 | 父项仍写「角色：<名字>」（这条自动化测试已经钉住了，真机上是复核）；或者勾也没了（那等于告诉用户他的选择丢了） | ❌ 同上（而且它会以错的方式红，见下） |
| 17 | 在 codex 里开一个终端 | 那句提示变成 **「下次启动 agent 时生效」**（不再是「可能要…」） | 两种宿主显示同一句 | ❌ 要 host_class |
| 18 | 把 `personas.json` 改出一个语法错误，重载 | 编辑器顶部出现**核心给的错误原文**；角色清单**还是上一份**（不是空） | 只显示「还没有定义任何角色」—— 那是让一个语法错误顶了别人的罪 | ✅ 能 |
| 19 | 让一个槽位的上游服务器起不来（`slot` 变成 `broken`） | 那一行写 **「这个服务器没有起来。不是你的角色没给你它。」** | 那一行看起来只是「没给」 | ❌ 要槽位进程 |
| 19b | 一个 `slot` 是 `granted` 或 `withheld` 的行 | **不额外写任何小字**——复选框已经说了 | 每一行都挂一句提示（那会把 19 的那句淹掉，而 19 是唯一非看不可的） | ✅ 能——今天每个槽位都是 `withheld`，正好是这一格 |
| 19c | 在 Polter 之外起的那种槽位（`slot` = `transparent`） | 写 **「Polter 没有接管这个服务器，所以 agent 看得到它的全部」** | 没有这句（用户会以为角色管住了它） | ❌ 同上 |
| 20 | 编辑器开着不动，在别处切换这个终端的角色 | 编辑器**自己跟着变** | 要关掉重开才更新 | ❌ 同上 |

---

# 已经不用人看的（自动化钉住了）

跑这一条就有答案：

```sh
xcodebuild -project macos/Ghostty.xcodeproj -scheme Ghostty -configuration Debug \
  -destination 'platform=macOS' test \
  -only-testing:GhosttyTests/PersonaMenuTests \
  -only-testing:GhosttyTests/PersonaDecodingTests \
  -only-testing:GhosttyTests/PersonaIconTests \
  -only-testing:GhosttyTests/PersonaMenuWidthTests \
  -only-testing:GhosttyTests/PersonaEditorSnapshotTests
```

45 条，`** TEST SUCCEEDED **`。（其中 31 条是原有的
`PersonaMenuTests` / `PersonaDecodingTests`，14 条是下面这三个文件。）

## 原来就有的（结构 / 措辞选择）

子菜单的项数与顺序、勾选落在哪一项、每行带的是不是自己的 key、护盾时哪些行禁用
哪些不禁用、冷 / unknown / 热三句各自出现与不出现、「没有 agent 连着时父项不声称
角色」、「已改的名字处处不同于没改的」、以及「还没有人报上来有哪些角色」和
「还没有定义任何角色」不许是同一句。

**这些断言证明过自己会红**：把 `agentPresent` 那个分支故意写反，红的是
`withNoAgentConnectedTheParentDoesNotClaimThePersona`、
`theParentNamesThePersonaWithoutBeingOpened`、
`aDeviatedPersonaReadsDifferentlyEverywhereItIsNamed` 三条。

## 新接管的三样，以及各自的地板

**没有地板的绿不算。** 下面每一格都是先把被测对象故意打坏、跑一遍、把红在哪一条
和原文抄下来，再把对象修回去。

### 1. 图标 —— `macos/Tests/Personas/PersonaIconTests.swift`（5 条）

判据是 `item.image != nil`，**两条 macOS 版本分支各判一次**。为了能判第二条，
`#available(macOS 26, *)` 被拆成了具名的 `NSMenuItem.menuItemImagesAreDesired`，
菜单builder 收一个 `imagesDesired` 参数（默认就是那个值，所以 app 里没有一个
调用点改过行为）。**这台机器永远只会走到 true 那条**，所以不拆开的话，
「26 以下一个图标都没有」这句话没有任何东西在证明它。

| 打坏什么 | 红在哪一条 | 原文 |
| --- | --- | --- |
| `PersonaSymbol.noRoles` 的名字改成 `"tray.no.such.symbol"` | `everySymbolTheRoleUINamesResolvesOnThisSystem`、`theEmptyListNoteCarriesItsOwnIcon` | 两条都 `failed`（前者点名 `tray.no.such.symbol resolved to no image`） |
| `setImage` 里 `if desired` 改成 `if true`（即门控失效） | `onASystemThatDoesNotWantIconsNothingHasOne` | 5 处断言，逐行点名，例如 `Expectation failed: (row.image → <NSImage … symbol = lock>) == nil: 不让 agent 碰此终端，所以它的角色也不能改 has an icon on a system that wants none` |

### 2. 文案截断 —— `macos/Tests/Personas/PersonaMenuWidthTests.swift`（2 条）

`NSAttributedString(…, .font: NSFont.menuFont(ofSize: 0)).size().width`
＋从真 `NSMenu.size` 标定出来的边框开销，对 640pt 预算；**每一种本地化都量一遍**。

地板打了两次，第一次没红，那一次同样是结论：

| 打坏什么 | 结果 |
| --- | --- |
| zh-Hans 那句改成 41 个汉字（607pt） | **仍然全绿** —— 没到 640。不是判据坏了，是我打得不够坏；顺带这给出了今天的余量 |
| 再加长到 56 个汉字（827pt） | `everyLocalizationOfEveryRowFitsTheBudget` 和 `theMenuFitsTheBudgetInTheLanguageThisMachineRunsIn` 双双红：<br>`Expectation failed: (total → 827.0) <= (Self.budget → 640.0): zh-Hans: "还没有人报上来这台机器上到底…" needs 827.0pt, over the 640.0pt budget` |

⚠️ **这个扫描自己带一道自检**：`translated >= 5`。没有它，一次「每种语言都查不到
译文、于是全都回落成英文」的扫描会**全绿**，而它跟真的扫过长得一模一样。

### 3. SwiftUI —— `macos/Tests/Personas/PersonaEditorSnapshotTests.swift`（7 条）

四张参考图在 `macos/Tests/Personas/__Snapshots__/`：满状态的亮/暗、
什么都没报上来的亮、以及最窄（460×420）那张。重录用
`POLTER_SNAPSHOT_RECORD=1`。

| 打坏什么 | 红在哪几条 | 原文 |
| --- | --- | --- |
| 删掉只读区那句「列在这里，是为了让你看见角色没有覆盖到哪里。Polter 不会动这些。」 | `anEditorWithARoleOnAndOneThingChangedByHand`、`theSameEditorInDarkMode`、`anEditorWithNothingReportedYet` | `editor-rich-light: 11650 of 613600 pixels differ (1.899%), over the 0.1% allowance`（暗 12400 / 空 3811） |

最窄那张**没有**红，这是对的：那句小字在 420pt 高度下本来就在 ScrollView 的
折叠线以下。

⚠️ **这一节的尺子自己坏过一次，值得记下来。** 第一版的合成方式（`drawn.draw(in:)`
画进一张带 alpha 的画布）把整张图留在了 **alpha 通道**里，RGB 平面两张图逐字节相同。
于是「删掉一整行字」的那次地板，亮色那两张报的是 **0 of 613600 pixels differ**——
**红的只有暗色一张，而我差点把它当成「地板成立」收工**。现在合成走
`NSImage.draw(…, operation: .sourceOver)`，比对连 alpha 一起比，并且多了一条
`everyRenderIsOpaqueSoTheComparisonIsLookingAtColour` 专门钉住这件事。
另外两条 `renderingTheSameModelTwiceGivesTheSamePixels` /
`twoDifferentModelsAreNotTheSamePicture` 是同一类守卫：一个防「渲染不可复现」，
一个防「在比两张空白」。

⚠️ **真窗口和这些参考图本来就有两处差，别把它们当回归。** 586 逐条比过：
真窗口有**标题栏和红绿灯按钮**，离屏渲染完全没有——那正是「窗口本体」和「窗口里
画的内容」的差本身；真窗口的「完成」是**蓝色默认按钮**，参考图里是灰的，因为
`.keyboardShortcut(.defaultAction)` 只在窗口是 key 时渲染成默认按钮，而离屏没有
key window。

⚠️ **还有一处是参考图的账，不是产品的**：真机默认状态是「空状态 + 暗色 + 最窄」，
而这四张里**没有一张是这个组合**（`empty` 只有亮色 520×1180，`narrow` 只有
rich + 亮色）。录参考图时选的点没打中真机第一眼看见的画面。

⚠️ **参考图是一台机器的录音。** 录于 macOS 26.5.1 / Xcode 26.6。换一个系统版本，
或者**换一个系统强调色**（复选框那一列跟随它，单选圈已经用 `.tint(.blue)` 钉死了），
都可能让它们变红而仓库里什么都没改。红了先看图（失败时会把实际图和参考图都写到
`/tmp`），不要条件反射地重录。

---

# 仍然只能人眼看的，以及各自为什么

**「这条判据不覆盖 X」是合格产出。** 下面每一条都说清楚机器卡在哪儿，而不是
「大概测不了」。

## 第一部分里剩下的

| # | 为什么机器判不了 |
| --- | --- |
| 0a / 0b | ⚠️ **这一行 2026-09-18 过期了**：584 建了 `tools/mac-drive.sh`，agent 现在**可以起自己那份 build-under-test 并按 pid 驱动它**（586 就是这么做的）。没变的是**绝不碰 `/Applications` 那个实例**——脚本对它是拒绝，没有覆盖开关。所以这一格剩下的只有「**确认眼前这个窗口来自新实例**」里要人眼的那部分；pid、cwd、窗口标题都能读，唯独「用户看见的是哪一个」要人看。 |
| 1（除图标外） | 「这一行真的挂在标签右键菜单上」是**接线**，不是建菜单。测试拿到的是 `PersonaMenu.makeItem` 的产物；`TerminalWindow` 有没有把它加进那个菜单，要么真去点一次，要么写一条会开窗口的测试——后者这一轮没做。 |
| 2 | 项数、顺序、勾、禁用态、三句措辞**已经全在** `PersonaMenuTests` 里。人这一侧剩下的只有「弹出来的那个菜单就是这份代码建的」，同第 1 条。 |
| 4 | 同上：两处菜单**由同一个 builder 建**（两处都调 `PersonaMenu.makeItem`，`TerminalWindow.swift` 和 `SurfaceView_AppKit.swift` 各一处）这件事是代码结构，能读；**弹出来的那两个菜单确实是它建的**是接线。 |
| 5 | **已经不是人眼那一格了**（586 用 `mac-drive.sh` 真开过一次，读数在第 5 条下面）。**留给人的只剩两样**：`完成` 按钮在 AX 里没有可读的 title（`description` 只是「按钮」），所以「按钮上写的是不是『完成』」今天只能看截图；以及那条**缺陷候选**——第二次点击既不开新窗也不提前——成因要人去读代码。 |
| 7（旧系统） | 这台机器上 11 个符号都取得到——**那是 26.5.1 答的账**。一个 macOS 14 才加的符号在这里 `!= nil`、在 13 上是空的，而这里没有 13 可跑。 |
| 8（判断那一半） | 快照能保证「不再变」，不能判断「现在这张对不对」。离屏录制时人看过一次，**真窗口在最窄处也看过一次**（586）——两次都是人看的，**没有机器判据说「这张图是好的」**。 |
| 9（可读性那一半） | **灰色小字读不读得清**是可读性判断。快照能抓住颜色变了，抓不住「变得看不清了」——要机器答得改成算对比度，没做。 |
| 9（另一种外观那一半） | 🔴 **586 没验，而且不是忘了。** 那天机器是 `AppleInterfaceStyle = Dark`，暗色那格真窗口验过；**亮色没有**。唯一能切的办法是改系统外观，而**用户正在用这台机器**；per-app 覆盖也不行——两个实例共用同一个 bundle id，覆盖会连用户那个一起改。⇒ **这是一条可测性限制，不是一次遗漏**：要填它，得让被测实例能被要求用某种外观启动（一个只许改可见性、不许改它经过哪些代码的开关）。 |
| 10 | 护盾时哪些行禁用、哪句提示出现，`PersonaMenuTests` 已经钉住；剩下的是「右键 →『不让 agent 碰此终端』这条路真的把 `shielded` 传进来了」，同第 1 条，是接线。 |

## 第二部分（写了 `personas.json` 之后）

**整段仍然是人的**，而且理由跟界面无关：11–20 里除 11 / 18 / 19b 之外，
今天要么核心没接（`_skill` / `_mcp` / `host_class` / 槽位进程），要么要真去改一个
真文件再重载。11 / 18 / 19b 今天能做但都要一份真的 `personas.json` 和一次真的重载，
这一轮没有把「造一份配置文件喂给核心」做成测试。

⚠️ 还有一条是原则性的，不要指望以后哪个测试能收掉：**第 12 行之后那半
「agent 手上到底还有没有那个工具」，判据必须在 agent 那边**（让它真去调用一个被
拿掉的工具）。界面说什么和 agent 拿到什么走的是两条路，这里的任何测试都只能答前者。
